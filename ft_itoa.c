char *itoa(int n);
char *ft_itoa(int n)
{
	char	*result;
	int	ch;
	int	d;

	ch = 1;
	d = 1;
	while (n / 10 > 10)
	result = (char *)malloc(ch * sizeof(char));
		ch++;
	if (result == 0)
		return (NULL);
	if (nb < 0)
	{
		result[i] = '-';
		i++;
	}
	if (nb < 9)
		result[i] = nb + '0';
	while (ch != 0)
		d *= 10;
		ch--;
	while (nb > 9 && i <)
	{
		result[i]
	}
}
